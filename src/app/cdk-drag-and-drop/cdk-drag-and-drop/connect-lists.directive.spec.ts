import { ConnectListsDirective } from './connect-lists.directive';

describe('ConnectListsDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectListsDirective();
    expect(directive).toBeTruthy();
  });
});
